package resonatefuse

import (
	"testing"
)

func TestFuse(t *testing.T) {
	// root := NewFile(NewDirectory("root", nil))
	// fs := NewFS("root")
	// fs.root = root

	// t.Log(root.node.ID())
	// root.Create("joe")
	// t.Log(root.node.Child("joe").ID())
	// root.node.Child("joe").CreateChild("show")
	// assert.Nil(t, root.node.Rename("joe/show", "show"))
	// t.Log(root.node.Child("joe").Dirents())
	// t.Log(root.node.Dirents())
	// t.Fail()
}
